---
"@1shotapi/ows-provider": patch
---

Release focus from the wallet iframe before collapsing it, and mark the host shell `inert` (then `aria-hidden`) so Chrome no longer warns when hiding after a WebAuthn ceremony.

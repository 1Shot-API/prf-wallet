---
"@1shotapi/ows-types": minor
"@1shotapi/ows-wallet-utils": minor
"@1shotapi/ows-provider": patch
---

Move shared EIP-1193 method lists and typed request tables into `@1shotapi/ows-types`. Add `RpcHelper` in `ows-wallet-utils` to register JSON-RPC read methods and chain switching on `OWSWallet` (replaces the registry rpc-provider module for branding apps).

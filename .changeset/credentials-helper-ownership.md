---
"@1shotapi/ows-oid4": minor
---

Refactor CredentialsHelper to SignHelper-style branding ownership: `approveAndAcceptOffer` / `approveAndPresent`, `release()` instead of `hide()`, and drop helper-level `ensureReady` / boolean approval hooks.

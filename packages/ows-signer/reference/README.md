# Reference material (not published)

1ShotPay wallet iframe reference copies for porting patterns. **Not included in npm or on-chain artifacts.**

- `walletFrame.ts` — PRF authentication flow (server-backed; do not port auth to ows-signer)
- `walletExample.ts` — Postmate RPC surface (wallet layer B, not custody signer)

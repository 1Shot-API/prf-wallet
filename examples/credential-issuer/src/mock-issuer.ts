import { MOCK_KYC_OFFER_URI } from "@1shotapi/ows-credentials/mock";

/** Returns the mock OID4VCI credential offer URI for demos. */
export function getMockIssuerOfferUri(): string {
  return MOCK_KYC_OFFER_URI;
}

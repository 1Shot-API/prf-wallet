{
  "name": "@1shotapi/ows-example-host",
  "version": "0.0.0",
  "private": true,
  "description": "OWS reference host application (layer A) — EIP-1193 via ows-provider",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "tunnel": "ngrok http 5173",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@1shotapi/ows-provider": "*"
  },
  "devDependencies": {
    "ngrok": "^5.0.0-beta.2",
    "vite": "^6.3.0"
  }
}

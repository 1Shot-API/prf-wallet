/**
 * OWS example wallet iframe (layer B).
 * Embeds custody signer via ows-signer-utils; talks to host via ows-wallet-utils.
 * Implementation forthcoming.
 */

console.info("[ows-example-wallet] scaffold loaded");
